services:
  agentos-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${IMAGE_NAME:-agno-agentos}:${IMAGE_TAG:-latest}
    container_name: agentos-api
    entrypoint: []
    command: >
      uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
      --reload-dir agents --reload-dir app --reload-dir db
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      # Hot-reload: monta il codice sorgente e mantiene persistente il DB SQLite sul tuo PC
      - .:/app
      - ./hf_cache:/app/hf_cache
    env_file:
      - path: .env
        required: false
    environment:
      - RUNTIME_ENV=dev
      - AGNO_DEBUG=True
      - PYTHONPATH=/app
      - AGENTOS_URL=http://127.0.0.1:8000
      - PRINT_ENV_ON_LOAD=True
      # API keys
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - HF_HOME=/app/hf_cache
    networks:
      - agentos
    extra_hosts:
      - "host.docker.internal:host-gateway"

    # --- ABILITAZIONE GPU NVIDIA ---
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  pgvector:
    image: agnohq/pgvector:18
    container_name: pgvector
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-agentos_db}
    volumes:
      - ./postgres_data:/var/lib/postgresql
    networks:
      - agentos

networks:
  agentos:
